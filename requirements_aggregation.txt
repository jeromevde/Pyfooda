# Dependencies for agentic food aggregation workflow
# Install with: pip install -r requirements_aggregation.txt

# Core dependencies (likely already installed)
pandas>=1.5.0
numpy>=1.24.0

# Embeddings and similarity
sentence-transformers>=2.0.0
scikit-learn>=1.0.0

# LLM API (optional - only needed if using --use-llm)
openai>=1.0.0

# Progress bars and utilities
tqdm>=4.65.0

# For SSL/certificate handling
requests>=2.28.0
